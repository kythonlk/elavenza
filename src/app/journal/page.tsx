import Link from 'next/link';
import Image from 'next/image';
import {journal} from '@/lib/journal';
export const metadata={title:'The wellness journal',description:'Explore thoughtful stories about everyday rituals, botanical skincare, slow living and gifting.',alternates:{canonical:'/journal'}};
export default function Page(){return <><section className="shop-hero"><div className="wrap"><Link href="/" className="breadcrumb">Home / The journal</Link><p className="eyebrow">NOTES FOR SLOWER LIVING</p><h1 className="shop-title">A little inspiration.<br/>A more intentional everyday.</h1><p>Stories, thoughtful details and simple rituals. Pour something lovely and stay a little while.</p></div></section><div className="wrap journal-index">{journal.map(p=><article key={p.slug}><Link href={`/journal/${p.slug}`}><div className="journal-index-image"><Image src={`/images/${p.image}.${p.image.startsWith('banner-')?'png':'jpg'}`} alt="" fill sizes="(max-width:760px) 100vw, 33vw" className="object-cover"/></div><div className="journal-index-copy"><p className="eyebrow">{p.category} / {Math.max(1,Math.ceil(p.sections.map(s=>s[1]).join(' ').split(/\s+/).length/200))} MIN READ</p><h2>{p.title}</h2><p>{p.intro}</p><span className="text-link">Read the story →</span></div></Link></article>)}</div></>}
