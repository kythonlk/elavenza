"use client";
import {useState} from 'react';
import Link from 'next/link';
import {subscribeNewsletterAction} from '@/lib/actions';
export default function NewsletterForm(){const [email,setEmail]=useState('');const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);const [done,setDone]=useState(false);return <section className="newsletter"><div className="wrap newsletter-inner"><div><span className="eyebrow">LET’S KEEP IN TOUCH</span><h2>A little goodness<br/>in your inbox.</h2><p>Join the Wellness Circle for thoughtful rituals, collection news and notes from Elavenza.</p></div><div>{!done&&<form onSubmit={async e=>{e.preventDefault();setBusy(true);setStatus('');try{const result=await subscribeNewsletterAction(email);if(!result.success)throw new Error();setDone(true);setStatus('You’re on the list. Thank you for joining our Wellness Circle.')}catch{setStatus('We couldn’t subscribe you just now. Please try again.')}finally{setBusy(false)}}}><label htmlFor="newsletter-email" className="sr-only">Email address</label><input id="newsletter-email" autoComplete="email" type="email" required maxLength={254} placeholder="Your email address" value={email} onChange={e=>setEmail(e.target.value)}/><button className="button" disabled={busy}>{busy?'Joining…':'Count me in →'}</button></form>}<p role="status">{status}</p><small>By joining, you agree to receive email updates. Read our <Link href="/privacy" className="underline">privacy information</Link>.</small></div></div></section>}
